services:
  dev:
    build:
      dockerfile: Dockerfile
      context: .
    volumes:
      - ./output:/output
    command:
      - bash
      - -c
      - "cp *.* /output && rm /output/build_kernel.sh"
