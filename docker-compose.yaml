services:
  dev:
    build:
      dockerfile: Dockerfile
      context: .
      args:
        VERSION: stable_20231123
    volumes:
      - ./output:/output
    command:
      - bash
      - -c
      - "cp *.* /output && rm /output/build_kernel.sh"
